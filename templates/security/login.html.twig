<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="{{asset('assets/css/login.css')}}">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
</head>
<body>
  {% block body %}
    <section class="vh-100">
       <div class="container-fluid vh-100">
         <div class="row">
            <div class="col-6">
                <div class="bg-light rounded" id="login-div">
                    <h1 class="d-flex justify-content-center">Se connecter</h1>
                    <form method="post">
                      <div class="mx-1">
                        {% if error %}
                          <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                        {% endif %}
                        <div class="form-floating mb-4 col-12">
                            <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control w-100" placeholder="name@example.com" autocomplete="email" required autofocus>
                            <label  for="inputEmail" class="px-4">Email</label>
                          </div>
                          <div class="form-floating mb-4 col-12">
                            <input type="password" name="password" id="inputPassword" class="form-control" placeholder="Password" autocomplete="current-password" required>
                            <label for="inputPassword"  class="px-4">Mot de passe</label>
                          </div>
                          <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
                        <div class="mt-3 d-flex justify-content-between" id="btm">
                            <button type="submit" class="btn btn-primary">Se connecter</button>
                            <a href="{{path('inscription')}}">Créer un compte ?</a>
                        </div>
                       </div>
                    </form>

                   <div class="social d-flex justify-content-between">
                    <div class="col-12">
                        <ul class="list-unstyled social">
                          <li><a href="#"><span class="fa-brands fa-facebook"></span></a></li>
                          <li><a href="#"><span class="fa-brands fa-twitter"></span></a></li>
                          <li><a href="#"><span class="fa-brands fa-instagram"></span></a></li>
                          <li><a href="#"><span class="fa-brands fa-linkedin"></span></a></li>
                        </ul>
                      </div>
                   </div>
                </div>
            </div>


            <div class="col-6" id="left-side">
                <svg width="700" height="500">
                    <image xlink:href="{{asset('assets/img/sign-in-animate.svg')}}" width="700" height="500" />
                </svg>
            </div>
        </div>
       </div>
    </section>
  {% endblock %}
</body>
</html>